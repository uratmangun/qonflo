{
  "name": "taskflow",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter @taskflow/backend dev\" \"pnpm --filter @taskflow/frontend dev\"",
    "build": "pnpm --filter @taskflow/backend build && pnpm --filter @taskflow/frontend build",
    "test": "pnpm --filter @taskflow/backend test"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3"]
  }
}
